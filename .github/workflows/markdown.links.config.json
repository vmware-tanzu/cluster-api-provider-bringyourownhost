{
    "ignorePatterns": [
        {
            "pattern": "^http://localhost"
        }
],
    "timeout": "30s",
    "retryOn429": true,
    "retryCount": 3,
    "fallbackRetryDelay": "30s",
    "aliveStatusCodes": [200, 206]
}
